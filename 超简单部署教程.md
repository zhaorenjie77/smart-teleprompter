# 🚀 超简单部署教程

> **目标：** 5 分钟内把你的智能提词器发布到网上，让手机可以访问！

---

## 📋 准备工作检查清单

✅ 已完成：
- [x] 获取 Gemini API Key: `AIzaSyC7DmATdV3gBJ9ED5e2MLhJU8DDmOJgsGw`
- [x] 本地代码已更新到最新版本
- [x] 代码已提交到本地 Git

⏳ 待完成：
- [ ] 推送代码到 GitHub
- [ ] 部署后端到 Railway
- [ ] 部署前端到 Vercel

---

## 第 1 步：推送到 GitHub（2 分钟）

### 在终端运行：

```bash
cd /home/jack/ZRJ
git push origin main
```

### 会提示输入：

1. **Username:** `zhaorenjie77`
2. **Password:** 使用你的 **GitHub Personal Access Token**（不是密码！）

### 如果没有 Token：

1. 访问：https://github.com/settings/tokens
2. 点击：**Generate new token** → **Generate new token (classic)**
3. 勾选：`repo` (所有选项)
4. 点击：**Generate token**
5. **复制 Token**（只显示一次！）
6. 粘贴到终端的 Password 提示处

---

## 第 2 步：部署后端到 Railway（2 分钟）

### 2.1 访问 Railway

🔗 **https://railway.app/new**

### 2.2 部署配置

1. **点击：** "Deploy from GitHub repo"
2. **选择：** `smart-teleprompter` 仓库
3. **点击仓库后：** 会自动开始部署

### 2.3 配置项目

部署开始后，点击项目进入设置：

#### ① 设置 Root Directory

1. 点击 **Settings** 标签
2. 找到 **Root Directory**
3. 输入：`backend`
4. 点击 **Update**

#### ② 添加环境变量

1. 点击 **Variables** 标签
2. 点击 **+ New Variable**
3. 添加：
   ```
   GOOGLE_API_KEY = AIzaSyC7DmATdV3gBJ9ED5e2MLhJU8DDmOJgsGw
   ```
4. 点击 **Add**

#### ③ 重新部署

1. 点击 **Deployments** 标签
2. 点击最新的部署
3. 点击右上角的 **⋯** → **Redeploy**

### 2.4 获取后端 URL

等待 3-5 分钟部署完成后：

1. 点击 **Settings** 标签
2. 找到 **Domains** 部分
3. 点击 **Generate Domain**
4. **复制生成的 URL**（例如：`https://smart-teleprompter-production.up.railway.app`）

📋 **保存这个 URL！下一步需要用！**

---

## 第 3 步：部署前端到 Vercel（1 分钟）

### 3.1 访问 Vercel

🔗 **https://vercel.com/new**

### 3.2 部署配置

1. **选择：** `smart-teleprompter` 仓库
2. **配置项目：**
   ```
   Framework Preset: Create React App
   Root Directory: frontend
   Build Command: npm run build
   Output Directory: build
   ```

### 3.3 添加环境变量

在 **Environment Variables** 部分：

```
REACT_APP_BACKEND_URL = <刚才 Railway 的后端 URL>
```

**例如：**
```
REACT_APP_BACKEND_URL = https://smart-teleprompter-production.up.railway.app
```

### 3.4 部署

点击 **Deploy** 按钮！

等待 2-3 分钟，部署完成后：

- 你会得到一个 Vercel URL（例如：`https://smart-teleprompter.vercel.app`）
- **这就是你的 App 地址！** 🎉

---

## 🎉 完成！开始使用

### 在手机上使用：

1. **打开手机浏览器**（Chrome/Safari）
2. **访问你的 Vercel URL**
3. **添加到主屏幕：**
   - **iOS:** 点击分享按钮 → "添加到主屏幕"
   - **Android:** 点击菜单 → "添加到主屏幕"

现在它就像一个真正的 App 了！📱

---

## 🔧 如果遇到问题

### 问题 1：Railway 部署失败

**解决方案：**
1. 确保 `Root Directory` 设置为 `backend`
2. 确保 `backend/nixpacks.toml` 文件存在
3. 查看部署日志，找到具体错误信息

### 问题 2：前端无法连接后端

**解决方案：**
1. 检查 Vercel 的环境变量 `REACT_APP_BACKEND_URL` 是否正确
2. 确保 Railway 后端已成功部署并生成了 Domain
3. 在 Vercel 中重新部署（Deployments → 最新部署 → Redeploy）

### 问题 3：API Key 配额限制

**解决方案：**
1. 等待 10-30 分钟让新 API Key 激活
2. 或者访问 https://aistudio.google.com/app/apikey 重新生成
3. 在 Railway 的 Variables 中更新新的 API Key
4. 重新部署

---

## 📝 快速命令参考

### 本地测试（可选）

```bash
# 测试 API Key
cd /home/jack/ZRJ
python3 测试Gemini_API.py

# 启动本地服务
./start.sh
```

### 更新部署

当你修改代码后：

```bash
# 1. 提交并推送
cd /home/jack/ZRJ
git add .
git commit -m "更新说明"
git push origin main

# 2. Railway 和 Vercel 会自动重新部署！
```

---

## 🎯 重要 URL 汇总

| 服务 | URL | 用途 |
|------|-----|------|
| GitHub 仓库 | https://github.com/zhaorenjie77/smart-teleprompter | 代码托管 |
| Railway 控制台 | https://railway.app/dashboard | 后端管理 |
| Vercel 控制台 | https://vercel.com/dashboard | 前端管理 |
| Gemini API 控制台 | https://aistudio.google.com/app/apikey | API Key 管理 |
| **你的 App** | `<Vercel 生成的 URL>` | **最终产品！** 🎉 |

---

## 💡 小贴士

1. **免费额度：**
   - Railway: 每月 $5 免费额度（足够测试使用）
   - Vercel: 无限免费部署
   - Gemini API: 每天免费配额（足够个人使用）

2. **自动部署：**
   - 每次推送到 GitHub，Railway 和 Vercel 都会自动重新部署
   - 不需要手动操作！

3. **分享给朋友：**
   - 直接把 Vercel URL 发给朋友
   - 他们可以直接在手机浏览器打开使用

---

**现在开始第 1 步吧！** 🚀

```bash
cd /home/jack/ZRJ
git push origin main
```

**有任何问题随时问我！** 💪
