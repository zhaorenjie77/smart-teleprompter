# Nixpacks configuration for Railway
# Railway 部署配置
# 使用轻量级版本以适应免费版限制

[phases.setup]
nixPkgs = ["python311"]

[start]
cmd = "uvicorn main_lite:app --host 0.0.0.0 --port $PORT --ws websockets --timeout-keep-alive 300"



